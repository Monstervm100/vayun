"""
train_model.py — trains and persists the Isolation Forest model (FR2.3, Section 6)
Run this once you have datasets/satellite_events.csv from simulator.py's
batch mode. Persist output with joblib to backend/models/.
"""
